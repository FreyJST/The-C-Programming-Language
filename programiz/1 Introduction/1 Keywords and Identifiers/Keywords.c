/*
 * some words have some special meaning to compiler that can't be used as identifers 
 * like int is a keyword and when we will write code we will use this as 
 *
 * int frey = 69;
 *
 * where int is a keyword which is int (integer) that indicates frey (variable) which is storing 69 in it :) 
 *
 * there are list of keywords please check them online 
 */
